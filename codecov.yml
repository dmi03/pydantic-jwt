coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 100%

comment:
  layout: "diff, flags, files"
  behavior: default
  require_changes: false
  hide_project_coverage: false
